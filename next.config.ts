import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["picsum.photos", "i.postimg.cc", "www.vasyerp.com"], // 👈 add your image host here
  },
};

export default nextConfig;
